import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "./components/Navbar";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OneDocs, Free Document Toolkit",
  description:
    "Analyze, convert, and manage PDF & DOCX documents entirely in your browser. No uploads, no accounts — 100% free.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Navbar />
        <div className="flex flex-col flex-1 overflow-hidden ruled relative" style={{ minHeight: "calc(100dvh - 54px)" }}>
          {children}
        </div>
      </body>
    </html>
  );
}
